
{{ form_start(form) }}
    {{ form_errors(form) }}
        {{ form_row(form.email) }}
        <br>
        {{ form_row(form.roles) }}
        <br>
        {{ form_row(form.plainPassword) }}
        <br>
    <br><button class="submit">{{ button_label|default('Enregistrer') }}</button><br>
{{ form_end(form) }}
