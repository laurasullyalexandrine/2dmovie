{% extends 'base.html.twig' %}

{% block title %}Edition du casting{{ casting.role }}{% endblock %}
{% block menu_casting %} active {% endblock %}
{% block body %}
<section>
    <div class="home-bloc-movie-picture-show">
        <div class="home-bloc-movie-information-show">
            <h1>Modifier : {{ casting.role }}</h1>

                {{ form_start(form) }}

                {{ form_errors(form) }}
<br>
                {{ form_row(form.role) }}
<br>

<br><br>
            <div class="admin-footer">
                    <button class="submit" type="submit">Modifier</button>
                    <a class="nav_admin"href="{{ path('admin_casting') }}"><button class="submit">Retour</button></a>
            </div> 
        </div>
    </div>
</section>
{{ form_end(form) }}

{% endblock %}