{% extends 'base.html.twig' %}

{% block title %}Ajout Casting{% endblock %}
{% block menu_casting %} active {% endblock %}

{% block body %}
<section class="bloc-admin-informations">
    <h2 class="entitled">Ajout acteurs.trices</h1>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_errors(form) }}
    <br>
    <span class="label-admin">{{ form_row(form.personage) }}</span>
    <br>
    <span class="label-admin">{{ form_row(form.creditOrder) }}</span>
    <br>
    <span class="label-admin">{{ form_row(form.movie) }}</span>
    <br>
    <div>
        <a class="nav_admin"href="{{ path('admin_casting_add')}}"><button class="submit" type="submit">Créer</button></a>
        <a class="nav_admin"href="{{ path('admin_genre') }}"><button class="submit">Retour</button></a>
    </div>
</section>
{{ form_end(form) }}
{% endblock %}
