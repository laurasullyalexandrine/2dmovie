{% extends 'base.html.twig' %}

{% block title %}Edition de l'acteur {{ person.name }}{% endblock %}
{% block menu_person %} active {% endblock %}
{% block body %}
<section>
    <div class="home-bloc-movie-picture-show">
        <div class="home-bloc-movie-information-show">
            <h1>Modifier : {{ person.name }}</h1>

                {{ form_start(form) }}

                {{ form_errors(form) }}
<br>
                {{ form_row(form.name) }}
<br>
                {{ form_row(form.castings) }}
<br>

<br><br>
            <div class="admin-footer">
                    <button class="submit" type="submit">Modifier</button>
                    <a class="nav_admin"href="{{ path('admin_person') }}"><button class="submit">Retour</button></a>
            </div> 
        </div>
    </div>
</section>
{{ form_end(form) }}

{% endblock %}