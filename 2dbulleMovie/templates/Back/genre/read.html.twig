{% extends 'base.html.twig' %}

{% block title %}Détails des genres{% endblock %}
{% block menu_genre %} active {% endblock %}
{% block body %}

<section class="container-admin">
    <div>
        <h2 class="entitled">Genre : <span class="movie">{{genre.name}}</span></h2>
        <h3 class="subtitle">Liste des films associés</h3>

{% for movie in genre.movies %}
<li class="category">{{movie.title}}</li>
{# afficher la liste des movies associer au genre (movie.id) et movie.title #}
 {% endfor %} 
    <div class="admin-footer">
        <a class="nav_admin" href="{{ path('admin_genre') }}"><button class="submit">Retour</button></a>
        <a class="nav_admin" href="{{ path('admin_genre_edit', {id : genre.id}) }}"><button class="submit">Editer</button></a>
        <a class="nav_admin" href="{{ path('admin_genre_delete', {id : genre.id}) }}"><button class="submit">Supprimer</button></a>
    </div>
</div>
</section>
{% endblock %}
